<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Project_MatField.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Project_MatField"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:Project_MatField.ViewModels"
    Title="Project-MatField"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TitleBar
            x:Name="titleBarMain"
            Title="HR Book"
            Subtitle="beta" />
        <NavigationView
            x:Name="navViewContent"
            Grid.Row="1"
            FlowDirection="RightToLeft"
            IsBackButtonVisible="Collapsed"
            PaneDisplayMode="Top">
            <interactivity:Interaction.Behaviors>
                <interactivity:EventTriggerBehavior EventName="SelectionChanged">
                    <interactivity:InvokeCommandAction Command="{x:Bind ViewModel.OnTabSelectedCommand}" />
                </interactivity:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
            <NavigationView.MenuItems>
                <NavigationViewItem
                    x:Name="navViewItemLibrary"
                    Content="کتابخانه"
                    CornerRadius="8"
                    Icon="Library"
                    IsSelected="True" />
                <NavigationViewItem
                    x:Name="navViewItemResearches"
                    Content="تحقیقات"
                    CornerRadius="8"
                    Icon="Document" />
            </NavigationView.MenuItems>
            <Frame x:Name="frameContent" FlowDirection="RightToLeft" />
        </NavigationView>
    </Grid>
</Window>
